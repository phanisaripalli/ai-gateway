# ===========================================
# AI Gateway - Environment Variables
# ===========================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# Usage:
#   Local dev:      source .env && ./gradlew bootRun
#   Docker:         docker run --env-file .env ...
#   Docker Compose: auto-loads .env from project root
# ===========================================

# --- Database (PostgreSQL) ---
DB_HOST=localhost
DB_PORT=5432
DB_NAME=ai_gateway
DB_USER=postgres
DB_PASSWORD=postgres

# --- Admin Account (created on first startup) ---
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=

# --- Security ---
# Generate with: openssl rand -base64 32
JWT_SECRET=
# Generate with: openssl rand -base64 32
ENCRYPTION_KEY=

# --- Provider API Keys (optional global fallback) ---
# Per-project keys can be added via dashboard
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GOOGLE_AI_API_KEY=
